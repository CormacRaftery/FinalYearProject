using System;
using System.Collections;
using System.Collections.Generic;
using System.Globalization;
using System.Reflection;
using UnityEngine;
using UnityEngine.UI;

public class PieceBehaviour : MonoBehaviour
{
}
    /*   // Start is called before the first frame update
       void Start()
       {

       }

       // Update is called once per frame
       void Update()
       {

       }*/
 /*   public Boolean PieceSelected = false;
    public Button button;
    public Button antiClockwiseButton;
    public Button clockwiseButton;
    public Text buttonText;
    public string playerSide;

    public void PickedPiece()
    {
        PieceSelected = true;
        if (antiClockwiseButton.pressed() && PieceSelected)
        {
            button.rotateImage("left");
        }
        if (clockwiseButton.pressed() && PieceSelected)
        {
            button.rotateImage("right");
        }
    }

    public void SetSpace()
    {
        buttonText.text = playerSide;
        button.interactable = false;
    }
}
 */